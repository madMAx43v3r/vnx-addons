package vnx.addons;

module MsgServer extends TcpServer {
	
	int compress_level = 0;					// zstd compression level (0 to 20, 0 = default)
	
	uint max_msg_size = 67108864;			// bytes
	
	ulong max_write_queue = -1;				// bytes per connection
	
}
